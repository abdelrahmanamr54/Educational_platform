@model Educational_platform.Models.Book

@{
    ViewData["Title"] = "ReadById";
}

<h1>ReadById</h1>
@*<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
*@
<div>
    <h4>Book</h4>
    <hr />
    <dl class="row  text-center">

     @*   <img  style="width:25%;height:auto" src="@Model.ImageUrl" class="" alt="..."  >*@
        <dt class="col-sm-2">
            Name
        </dt>
        <dd class="col-sm-10">
            @Model.Name
        </dd>
        <dt class="col-sm-2">
            Price
        </dt>
        <dd class="col-sm-10">
            @Model.Price
        </dd>
        <dt class="col-sm-2">
            grade
        </dt>
        <dd class="col-sm-10">
            @Model.grade.Name
        </dd>
    </dl>
</div>
<div>
    <a asp-controller="Cart" asp-route-id="@Model?.Id" asp-action="Download">Download the Book</a>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
